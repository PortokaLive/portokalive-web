import React, { useEffect, useState } from "react";
import { getMediaServerUrl, getToken } from "../../utils/constants";

export const LiveStreamView = ({ history }: any) => {
  return (
    <>
      <div className="row no-gutters mt-5 pt-5 overflow-hidden"></div>
    </>
  );
};
